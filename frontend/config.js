// Placeholder. At deploy time the CDK BucketDeployment overwrites this file
// with the real API URL:  window.FEEDBACK_API = "https://.../prod/";
// When this placeholder is what loads (e.g. opening the site locally before a
// deploy), FEEDBACK_API stays undefined and the wall runs in demo mode.
window.FEEDBACK_API = window.FEEDBACK_API || "";
