{
  "name": "cdk-skip-the-wait",
  "version": "1.0.0",
  "description": "AWS CDK - Skip the wait. DevCon 2026 booth demo: a live feedback wall app showcasing CDK speed features (synth-performance skill, synth-time validation, hotswap, express mode).",
  "private": true,
  "bin": {
    "cdk-skip-the-wait": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "cdk synth",
    "clean": "rm -rf cdk.out lib/**/*.js lib/**/*.d.ts bin/*.js bin/*.d.ts *.tsbuildinfo"
  },
  "devDependencies": {
    "@cdklabs/cdk-validator-cfnguard": "^0.0.62",
    "@types/node": "^22.9.0",
    "aws-cdk": "^2.1130.0",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  }
}
